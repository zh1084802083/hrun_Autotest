# NOTE: Generated By HttpRunner v3.1.6
# FROM: testcases/Statistics/rental/contract_lease_count.yml


import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent.parent.parent))


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase

from testcases.Buildings.buildings_test import TestCaseBuildings as Buildings


class TestCaseContractLeaseCount(HttpRunner):

    config = (
        Config("数据舱--全年退租合同份数/面积/工位数")
        .variables(
            **{
                "endDate": "2021-12-31",
                "startDate": "2021-01-01",
                "access_token": "${get_token()}",
            }
        )
        .export(*["building_id"])
    )

    teststeps = [
        Step(RunTestCase("获取楼宇id").call(Buildings).export(*["building_id"])),
        Step(
            RunRequest("全年退租合同份数--房源")
            .get("${ENV(api_url)}/data-statistics/rent-analyze/growth-churn-ratio")
            .with_params(
                **{
                    "buildingIds": "$building_id",
                    "churnType": "LEASE_EXPIRED,RENT_OUT_EARLY",
                    "endDate": "$endDate",
                    "startDate": "$startDate",
                    "statisticSpaceType": "ROOM",
                    "statisticsType": "CONTRACT_COUNT",
                    "temporalUnit": "YEAR",
                }
            )
            .with_headers(**{"authorization": "Bearer $access_token"})
            .validate()
            .assert_equal("status_code", 200)
        ),
        Step(
            RunRequest("全年退租合同面积--房源")
            .get("${ENV(api_url)}/data-statistics/rent-analyze/growth-churn-ratio")
            .with_params(
                **{
                    "buildingIds": "$building_id",
                    "churnType": "LEASE_EXPIRED,RENT_OUT_EARLY",
                    "endDate": "$endDate",
                    "startDate": "$startDate",
                    "statisticSpaceType": "ROOM",
                    "statisticsType": "AREA_SIZE",
                    "temporalUnit": "YEAR",
                }
            )
            .with_headers(**{"authorization": "Bearer $access_token"})
            .validate()
            .assert_equal("status_code", 200)
        ),
        Step(
            RunRequest("全年退租合同份数--工位")
            .get("${ENV(api_url)}/data-statistics/rent-analyze/growth-churn-ratio")
            .with_params(
                **{
                    "buildingIds": "$building_id",
                    "churnType": "LEASE_EXPIRED,RENT_OUT_EARLY",
                    "endDate": "$endDate",
                    "startDate": "$startDate",
                    "statisticSpaceType": "CUBICLE",
                    "statisticsType": "CONTRACT_COUNT",
                    "temporalUnit": "YEAR",
                }
            )
            .with_headers(**{"authorization": "Bearer $access_token"})
            .validate()
            .assert_equal("status_code", 200)
        ),
        Step(
            RunRequest("全年退租工位数--工位")
            .get("${ENV(api_url)}/data-statistics/rent-analyze/growth-churn-ratio")
            .with_params(
                **{
                    "buildingIds": "$building_id",
                    "churnType": "LEASE_EXPIRED,RENT_OUT_EARLY",
                    "endDate": "$endDate",
                    "startDate": "$startDate",
                    "statisticSpaceType": "CUBICLE",
                    "statisticsType": "AREA_SIZE",
                    "temporalUnit": "YEAR",
                }
            )
            .with_headers(**{"authorization": "Bearer $access_token"})
            .validate()
            .assert_equal("status_code", 200)
        ),
        Step(
            RunRequest("全年退租合同份数--房源+工位")
            .get("${ENV(api_url)}/data-statistics/rent-analyze/growth-churn-ratio")
            .with_params(
                **{
                    "buildingIds": "$building_id",
                    "churnType": "LEASE_EXPIRED,RENT_OUT_EARLY",
                    "endDate": "$endDate",
                    "startDate": "$startDate",
                    "statisticSpaceType": "ALL",
                    "statisticsType": "CONTRACT_COUNT",
                    "temporalUnit": "YEAR",
                }
            )
            .with_headers(**{"authorization": "Bearer $access_token"})
            .validate()
            .assert_equal("status_code", 200)
        ),
        Step(
            RunRequest("全年退租合同面积--房源+工位")
            .get("${ENV(api_url)}/data-statistics/rent-analyze/growth-churn-ratio")
            .with_params(
                **{
                    "buildingIds": "$building_id",
                    "churnType": "LEASE_EXPIRED,RENT_OUT_EARLY",
                    "endDate": "$endDate",
                    "startDate": "$startDate",
                    "statisticSpaceType": "ALL",
                    "statisticsType": "AREA_SIZE",
                    "temporalUnit": "YEAR",
                }
            )
            .with_headers(**{"authorization": "Bearer $access_token"})
            .validate()
            .assert_equal("status_code", 200)
        ),
    ]


if __name__ == "__main__":
    TestCaseContractLeaseCount().test_start()
